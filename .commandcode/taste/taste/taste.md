# Taste
- Requires every public class, property, and method to carry `dartdoc` (`///`) comments. Confidence: 0.85
- Forbids placeholder/stub comments (e.g. `// TODO: implement rest`); expects complete, fully compilable code rather than partial scaffolding. Confidence: 0.85
- Prefers pulling styling dynamically from a theme provider (e.g. `KumoTheme.of(context)`) instead of inlining raw hex/color values at call sites, including text: components resolve their `TextStyle` colors from the active scheme rather than applying styles with baked-in tones. Confidence: 0.8
- Avoids generic Material/Cupertino fallbacks; restricts imports to `package:flutter/widgets.dart` (no `material.dart` or `cupertino.dart`). Confidence: 0.8
- Expects `flutter analyze` to run clean (zero static analysis errors) and `flutter test` to pass, across both the library and example packages, as explicit completion criteria before a task is considered done. Confidence: 0.85
- Commits to explicit task specifications with named file locations, exact parameters (pixel sizes, hex values, API signatures), constraints, and acceptance steps. Confidence: 0.7
- For a UI library, expects an interactive example app (`example/lib/main.dart`) that showcases every component, kept in sync as new components are added. Confidence: 0.55
- Prefers mobile-first adaptive layouts that respond to available width: a stacked arrangement on phones and a side-by-side/grid arrangement on wide containers, chosen via `LayoutBuilder`/`MediaQuery` around a shared ~600px breakpoint, with full-width touch targets of at least 48px. Confidence: 0.6
- Prefers responsiveness delivered as a single adaptive widget that switches behavior at a breakpoint (e.g. docked rail on desktop, drawer/sheet with scrim on mobile) rather than shipping separate mobile and desktop widgets; asks for one widget to be attempted first, with the adaptive code path required if a single widget genuinely cannot cover both. Confidence: 0.6
- Avoids Material-only APIs and constants in widgets-only code, e.g. uses `const Color(0x00000000)` instead of `Colors.transparent`. Confidence: 0.7
- Expects new components to ship with widget tests, and the example app to have a test that compiles and renders it (rather than trusting `analyze` alone). Confidence: 0.6
- Requires an accessibility contract on interactive widgets: `Semantics` tags on every clickable control (button/toggled/selected/expanded/enabled state and label), a keyboard focus ring shown on focus change, and `BoxConstraints(minWidth: 48, minHeight: 48)` minimum touch targets on all interactive components. Confidence: 0.85
- Requires color combinations used for text to clear WCAG AA contrast (>= 4.5:1) against every background they are painted on, with the weaker 3:1 threshold reserved for non-text indicators; expects the ratios to be measured, not assumed. Confidence: 0.85
- Prefers design tokens named by semantic role (canvas/surface/border/textPrimary/dangerText) over raw hue ramps, and accepts a documented role model in place of a literal N-step gray scale. Confidence: 0.6
- Prefers work to be grounded in the canonical upstream reference (official docs/site) and any spec premise that conflicts with it to be flagged and reported rather than satisfied by inventing values; expects component availability to be validated against the upstream design system's own component set (e.g. Cloudflare's Kumo registry/Figma reference) rather than assumed. Confidence: 0.6
- Prefers single-import ergonomics for a library: re-exports the core Flutter primitives it depends on (from `package:flutter/widgets.dart`) so a consumer can build a screen with only `import 'package:kumo_ui/kumo_ui.dart';`, while still refusing to re-export any Material/Cupertino symbols. Confidence: 0.6
- Keeps source files organized into clean, clearly commented sections (anti-slop code cleanliness) grouped by concern (primitives, components, theming) rather than a flat dump of declarations. Confidence: 0.6
- Releases follow conventional-commit messages with a type/scope prefix (e.g. `feat(dx): ...`), an annotated semver git tag (`git tag -a vX.Y.Z -m ...`), and pushing the branch plus tags to origin. Confidence: 0.6
- Targets a maximum pub.dev score: full README with badges, a platform-support table, copy-pasteable usage examples and a component catalog, plus complete public dartdoc; aims for a warning-free `pub publish` that follows pub layout conventions (e.g. the singular `doc/` directory) and uses absolute `raw.githubusercontent.com` URLs for README images so they render on pub.dev. Confidence: 0.6
- Expects user-facing documentation (README, CHANGELOG, version badges, install snippets) — plus the bundled agent skills under `skills/*/` (their version metadata and install snippets) — to be kept in sync with new features and configuration changes in the same pass, leaving no stale version numbers, repo URLs, or outdated claims behind. Confidence: 0.6
- Expects a release to be carried end-to-end in one task: bump the version, sync the docs, commit + annotate-tag + push to GitHub, and run the real publish to pub.dev (not just a `--dry-run`). Confidence: 0.7
- Audits a package README's feature catalog against the actual public API surface (the barrel export file) so every exported widget, enum and token is documented, instead of trusting the prose that is already there. Confidence: 0.7
- Wants screenshots of the bundled example app embedded in the README as a visual gallery (thumbnails linking to full-size images), not just linked or omitted. Confidence: 0.55
- Prefers additive, non-breaking API evolution: new capability ships as a minor release that keeps existing constructors, statics and consumer call sites working, deferring a type-level refactor rather than breaking it. Confidence: 0.55
- Keeps a library unopinionated: app-level policy and state (e.g. theme-mode selection) belongs in the example or the consumer app, not in the package's public API. Confidence: 0.5
- Expects light and dark color schemes to both be fully supported and legible, not one scheme with a mechanical token swap. Confidence: 0.55
- Expects shipped variants (e.g. each color scheme) to be asserted by the automated test suite so a regressing variant fails the build, rather than being verified by eye. Confidence: 0.6
- Applies anti-slop UI discipline: no gradients, glows, glassmorphism, background grids or decorative arrows; icons chosen for their meaning; specific, non-buzzword CTA labels; corner radii varied by role. Names the anti-slop skills explicitly in prompts, expecting them to be invoked/enforced rather than treated as background guidance. Confidence: 0.55
- Prefers ergonomic, MaterialApp-style entry points: collapses mandatory composition boilerplate (theme + WidgetsApp + route builder + MediaQuery wiring) into a single convenience widget so consumers write one widget instead of a nested tree (e.g. `KumoApp(home: ...)`). Confidence: 0.7
- Expects a convenience app wrapper to reach full parity with the framework API it imitates, not a trimmed subset: when the framework offers both a default and a `.router` constructor, the wrapper ships both (`KumoApp` / `KumoApp.router`) and exposes the sibling's whole parameter surface — including the raw Navigator 2.0 delegates (`RouteInformationProvider`, `RouteInformationParser`, `RouterDelegate`, `BackButtonDispatcher`) alongside `routerConfig` — with the same parameters deliberately omitted and the same mutual-exclusion rules. Confidence: 0.7
- Verifies a release externally after the fact (querying the pub.dev API for the live version, resolving the published image URLs) rather than trusting the publish command's exit status. Confidence: 0.55
- Writes terse, loosely-punctuated prompts with `@file` references that bundle several related asks in one message, expecting the agent to decompose them. Confidence: 0.55
- Asks the agent to "validate first" — before building a feature, verify the premise/feasibility against the actual pinned SDK source and the upstream packages' real APIs (constructors, config types, required wiring), then report the verdict and remaining gaps rather than starting implementation on an unchecked assumption. Confidence: 0.7
- Expects the library to interoperate with standard Flutter ecosystem conventions (e.g. Navigator 2.0 / `RouterConfig`-driven routing for go_router and auto_route) instead of being a closed island, so third-party packages work against its app entry point. Confidence: 0.65
- Keeps agent-facing instruction files (e.g. `AGENTS.md`) aligned with the actual implemented project — rewriting stale/aspirational prose to match the real widgets and APIs, while preserving the file's original core principles. Confidence: 0.6
- Wants distributable agent tooling (skills) authored to an open, agent-agnostic standard (`SKILL.md` with YAML frontmatter) so any coding agent can install and use it, rather than binding tooling to one vendor; expects other developers adopting the package to be able to install it easily. Confidence: 0.7
- Prefers a library-native screen shell (e.g. `KumoScaffold`) so consumers stop repeating canvas + safe-area + scroll boilerplate, while keeping the implementation built from the library's own primitives rather than importing the framework widget. Confidence: 0.6
- Prefers validation and tooling that require no personal account credentials or secrets, choosing a vendored offline inventory plus a drift-check script over an integration needing a Figma personal access token and access to a vendor design file. Confidence: 0.55
- Expects component parity with the upstream design system rather than a trimmed subset: when offered a minimal tranche versus implementing the full remaining upstream set, picks the full set, accepts that it is a multi-release effort, and asks for the whole unimplemented list in one request rather than naming individual components. Confidence: 0.55
- Wants implementation status surfaced as an explicit checkbox checklist in the README (`- [x]` done / `- [ ]` not yet), enumerating the upstream component set with implemented, partial, missing and deliberately-excluded groups, each entry mapped to the concrete widget that covers it. Confidence: 0.5
- Expects their own authorship to be preserved in git history — agent-authored commits should credit the user as author/co-author (not only a bot trailer), so the user appears on GitHub's contributor graph. Confidence: 0.55
- Willing to rewrite published git history to correct commit metadata — accepts rewriting author and committer across all commits and force-pushing `main` with re-pointed tags, prioritizing correct attribution over history immutability. Confidence: 0.5
- Keeps the agent's `Co-authored-by` bot trailer on its commits alongside their own credit rather than stripping the bot from the contributor list, treating the shared attribution as honest. Confidence: 0.45
- Prefers a pure Dart rendering path for visualizations — hand-written `CustomPainter` over low-level `Canvas`/`Path`/`Paragraph` APIs — and rules out `WebView`, JavaScript bridges, or wrapping third-party charting packages, so the package stays dependency-free. Confidence: 0.6
- Demands frame-budget performance discipline in custom painters: zero object allocations inside `paint()` (pre-allocated `Paint`/`Path`/`TextPainter` held as fields), `RepaintBoundary` splitting that isolates static layers (grid, axes, tick marks) from the animated data overlay, spatial index (`RTree`/`QuadTree`) hit testing, and downsampling (LTTB) so rendered points match pixel resolution. Confidence: 0.6
- Prefers stream-native, push-driven data flow for live data: widgets bind to a controller/`Listenable` and coalesce bursts into a single flush per frame using fixed-capacity ring buffers with no per-tick allocation, rather than `setState`/rebuild per stream message. Confidence: 0.6
- Expects performance targets to be validated by measurement and profiling (DevTools CPU profiler, cost per downsample, allocation count per append) rather than asserted as frame-rate numbers. Confidence: 0.5
- Splits performance verification by axis: the user does manual FPS profiling on real hardware and explicitly forbids the agent from running synthetic FPS benchmarks; the agent's optimization responsibility is pure-Dart efficiency (zero allocations in `paint()`, microsecond-scale downsampling, minimal draw-call overhead). Confidence: 0.65
- Holds the version number and publish step until a feature area is fully implemented and verified — no intermediate release claiming a partially built subsystem, even if the completed slice is independently useful. Confidence: 0.6
- Relaxes an earlier-stated constraint with an explicit grant when it would otherwise block progress, and expects the agent to proceed under that grant rather than pausing to ask for permission — while still reporting whether the original constraint was actually sufficient. Confidence: 0.5
- Decomposes a feature into pure-Dart-first modules with staged execution: viewport/coordinate/projection math is required to live in a module with zero canvas, widget or `BuildContext` dependencies (imports limited to `dart:typed_data`, `dart:math` and geometry/vector-math types), so the logic is unit-testable in isolation before the painter/widget layer is written. Confidence: 0.6
- Prefers a sliding viewport implemented as a translation update on a `Matrix4` (updating translation offsets) rather than re-indexing or re-projecting historical point arrays as new streaming ticks arrive. Confidence: 0.55
- Requires bulk/loop projection APIs to accept caller-owned, pre-allocated `Float64List` output buffers and be mutated in place (returning a written count rather than a newly allocated list), and expects this to be asserted by a test. Confidence: 0.6
- Enumerates degenerate numerical inputs as first-class requirements — zero elapsed time (`min == max`), a flat value domain, empty buffers and out-of-range values — expecting each to resolve to a defined result (e.g. centre-line) instead of dividing by zero / producing NaN, covered by boundary-case unit tests. Confidence: 0.55
- For data-visualization components, expects a fixed, documented chart palette rather than improvised colors: line series pull sequentially from a categorical set cycled by index (e.g. `categorical[i % 5]`) or explicit semantic tokens (`success`/`warning`/`error`), gridlines use a 1px stroke on the hairline border token, axis ticks/values use the lowest-emphasis (muted) caption text token, and area fills under a line fade to 0% alpha at the baseline — functional chart grids/gradients are treated as exceptions to the general no-gradient/anti-slop rule. Confidence: 0.55
- Requires algorithmic/graph modules to be defensive against structurally invalid input: self-referential or cyclic links must be handled gracefully and terminate rather than hang, with an explicit cycle-guard test, and dangling/unresolvable endpoints tolerated (link dropped) instead of throwing. Confidence: 0.55
- Expects every required test verification to be genuinely assertable: is comfortable adding small public diagnostic getters so an internal pipeline stage (e.g. the downsample count actually handed to the rasteriser) can be tested rather than assumed, and expects requirements that cannot be directly asserted (e.g. "zero heap allocations", unobservable from Dart) to be reported honestly with the observable substitute spelled out instead of faked. Confidence: 0.5
- Wants first-class low-level escape hatches in the public API for advanced consumers: a composable callback/typedef (e.g. `void Function(Canvas canvas, Size size, KumoChartContext context)`) that hands developers raw-canvas control while the library still manages the surrounding gridlines, axis ticks, theme tokens and repaint split, with a fully pre-computed context passed in so custom paint callbacks never perform theme lookups or layout math. Confidence: 0.5
- Expects the example app to exercise declarative routing (Navigator 2.0 / `RouterDelegate`) with deep-linkable per-view paths, driven through the library's own navigation shell — drawer navigation updates the route and the active-item highlight stays synced to the active route path — rather than a plain Navigator stack. Confidence: 0.45
- Prefers public API lookups to degrade gracefully instead of crashing: a static helper resolved from the wrong context (an ancestor rather than a descendant) should assert in debug with an actionable message that names the cause and the fix, and no-op (or return a safe default) in release rather than throwing a bare null-check `TypeError`; expects a separate non-asserting probe (e.g. `hasScaffold`) for callers that may be rendered outside the scope. Confidence: 0.55
- Treats task directives as hypotheses rather than literal specs: the directive's illustrative code often carries API names and parameters that do not match the real code (non-existent state classes, a `title:` where the real parameter is `label:`, a `String`-taking parser where the real API takes a decoded map), so the agent is expected to verify each premise against the source, keep the real API and file names, and report the deviations it declined to implement instead of inventing types with no consumer. Confidence: 0.5
- Uses repo-mapping graph tooling (a `/graphify`-style skill that builds a knowledge graph into `graphify-out/` with `graph.json`, `GRAPH_REPORT.md` and an interactive `graph.html`) to survey a codebase alongside large feature work. Confidence: 0.4
 that builds a knowledge graph into `graphify-out/` with `graph.json`, `GRAPH_REPORT.md` and an interactive `graph.html`) to survey a codebase alongside large feature work. Confidence: 0.4
- Treats package-registry metadata (pub.dev) as first-class published copy, not just the README: a benefit-oriented `description:` that says what the package gives you rather than the code's shape, kept inside the registry's length limit (pub.dev's 60–180 chars), plus the full set of up to 5 lowercase hyphenated `topics` so the package is discoverable by category, and a publisher-page name/blurb. Confidence: 0.55
- Keeps volatile figures (component/widget counts) out of immutable or version-pinned copy such as a published package description, since the number drifts while a published description cannot be edited for an already-shipped version; prefers count-free phrasing and corrects any stale count found elsewhere in the docs to the real, measured value. Confidence: 0.6
